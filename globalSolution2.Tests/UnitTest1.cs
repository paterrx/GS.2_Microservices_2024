using Xunit;

public class HealthTests
{
    [Fact]
    public void Health_Should_Return_Healthy_Status()
    {
        // Simula a chamada à API.
        Assert.True(true); // Substituir por simulação real.
    }
}
